model Product {
  id          String  @id @default(uuid())
  item_code   String
  description String
  category    String
  unit_price  Float
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt
}